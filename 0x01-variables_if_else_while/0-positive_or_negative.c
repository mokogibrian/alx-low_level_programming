#include <stdlib.h>
#include <time.h>

/* This program returns the value of 0, whether positive or negative */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 0)
		(%d\n, n is positive);
	else if  (n==0)
		(%d\n, n is zero);
	else 
		(%d\n, n is positive
	return (0);
}
