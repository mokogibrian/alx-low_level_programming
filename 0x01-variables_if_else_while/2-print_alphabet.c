#include <stdio.h>

/**
 * main - Prints alphabet in lower case
 *
 * Return - always zero (Success)
 */

int main(void)
{
	char ch

	for (ch = 'a'; ch <= 'z'; ch++)

	{
		putchar(ch);

	}

	putchar('\n');

	return (0);

}
